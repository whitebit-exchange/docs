---
title: Place stop-market order
tag: "WSS"
description: "Place a spot stop-market order that fires a market order on trigger, with order_stop_market_place."
---

import { RegionBaseUrl } from '/snippets/RegionBaseUrl.jsx';
import { WsChannelOverview } from '/snippets/WsChannelOverview.jsx'
import { WsSchemaTable } from '/snippets/WsSchemaTable.jsx'
import { WsMessageExample } from '/snippets/WsMessageExample.jsx'
import { WsAuthBadge } from '/snippets/WsAuthBadge.jsx'
import { WsRateLimits } from '/snippets/WsRateLimits.jsx'
import { WsErrorCodes } from '/snippets/WsErrorCodes.jsx'
import {
  channelOperations,
  channelMeta,
  orderStopMarketPlaceParams,
  exOrderStopMarketPlaceRequest,
  exOrderStopMarketPlaceResponse,
} from '/snippets/ws-data/order_stop_market_place.jsx'

<RegionBaseUrl protocol="wss" />

Fire a market order when the market reaches `activation_price`. Because the resulting order carries no limit price, `price` is reported as `"0"` both while the order waits and after it executes. This method trades price certainty for fill certainty — the opposite of [`order_stop_limit_place`](/websocket/order-management/order-stop-limit-place).

<WsChannelOverview operations={channelOperations} />

<WsAuthBadge required={channelMeta.authRequired} />

## Rate limits

<WsRateLimits {...channelMeta.rateLimits} />

## Place stop-market order

### Request parameters

<WsSchemaTable fields={orderStopMarketPlaceParams} />

The unit of `amount` follows the market-order convention: [money](/glossary#money) on a buy, [stock](/glossary#stock) on a sell. For stock sizing on both sides use [`order_stop_market_stock_place`](/websocket/order-management/order-stop-market-stock-place).

Unlike the stop-limit trigger, `activation_price` here is additionally validated against the market price bands. A trigger outside those bands returns error `251`.

<WsMessageExample data={exOrderStopMarketPlaceRequest} />

### Response

<WsMessageExample data={exOrderStopMarketPlaceResponse} />

The `result` object adds the conditional fields to the standard order object — see the [order object reference](/websocket/order-management/overview#order-object). A spot stop-market order reports `type` `4`.

<Note>The example sends `amount` as `"3.00"` and the response echoes `"3"`. Trailing zeros are normalized on the way out, so compare decimal values numerically rather than by string equality.</Note>

## Error codes

<WsErrorCodes errorCodes={channelMeta.errorCodes} />

Standard transport errors and the shared account, rate-limit, and region codes are listed in [Order Management](/websocket/order-management/overview#error-codes).
