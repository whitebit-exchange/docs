---
title: Place stop-BBO order
tag: "WSS"
description: "Place a spot stop order that becomes a BBO order on trigger, with order_stop_bbo_place."
---

import { RegionBaseUrl } from '/snippets/RegionBaseUrl.jsx';
import { WsChannelOverview } from '/snippets/WsChannelOverview.jsx'
import { WsSchemaTable } from '/snippets/WsSchemaTable.jsx'
import { WsMessageExample } from '/snippets/WsMessageExample.jsx'
import { WsAuthBadge } from '/snippets/WsAuthBadge.jsx'
import { WsRateLimits } from '/snippets/WsRateLimits.jsx'
import { WsErrorCodes } from '/snippets/WsErrorCodes.jsx'
import {
  channelOperations,
  channelMeta,
  orderStopBboPlaceParams,
  exOrderStopBboPlaceRequest,
  exOrderStopBboPlaceResponse,
} from '/snippets/ws-data/order_stop_bbo_place.jsx'

<RegionBaseUrl />

Park an order that turns into a BBO order when the market reaches `activation_price`. The price is resolved from the order book at activation time rather than at placement, which is why `price` stays `"0"` while the order waits.

<WsChannelOverview operations={channelOperations} />

<WsAuthBadge required={channelMeta.authRequired} />

<Note>Connect to `wss://api.whitebit.com/ws` and [authorize](/websocket/account-streams/authorize) the connection first. Shared conventions, the order object, and the full error reference are in [Order Management](/websocket/order-management/overview).</Note>

## Rate limits

<WsRateLimits {...channelMeta.rateLimits} />

## Place stop-BBO order

### Request parameters

<WsSchemaTable fields={orderStopBboPlaceParams} />

The `bbo_role` semantics match [`order_bbo_place`](/websocket/order-management/order-bbo-place), with one difference: the role is applied at activation, against the order book as it stands then. Role `1` sets `post_only` on the resulting order.

<WsMessageExample data={exOrderStopBboPlaceRequest} />

### Response

<WsMessageExample data={exOrderStopBboPlaceResponse} />

The `result` object adds the conditional fields to the standard order object and reports `type` `3` with `price` `"0"`. See the [order object reference](/websocket/order-management/overview#order-object).

<Warning>An empty order book at activation drops the order. With no quote on the required side there is nothing to peg to, so the order cannot be priced and is discarded rather than retried. Use [`order_stop_limit_place`](/websocket/order-management/order-stop-limit-place) where a guaranteed price matters more than a book-relative one.</Warning>

## Update frequency

This method is client-initiated: the server answers each `order_stop_bbo_place` request with a single response frame carrying the same `id`. Subscribe to [Orders Pending](/websocket/account-streams/orders-pending) to observe the trigger firing and the resolved price.

## Error codes

<WsErrorCodes errorCodes={channelMeta.errorCodes} />

Standard transport errors and the shared account, rate-limit, and region codes are listed in [Order Management](/websocket/order-management/overview#error-codes).
