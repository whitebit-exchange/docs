---
title: Positions
tag: "WSS"
description: "Subscribe to real-time collateral position updates for a WhiteBIT account via WebSocket."
---

import { WsChannelOverview } from '/snippets/WsChannelOverview.jsx'
import { WsSchemaTable } from '/snippets/WsSchemaTable.jsx'
import { WsMessageExample } from '/snippets/WsMessageExample.jsx'
import { WsSubscribeSteps } from '/snippets/WsSubscribeSteps.jsx'
import { WsAuthBadge } from '/snippets/WsAuthBadge.jsx'
import { WsRateLimits } from '/snippets/WsRateLimits.jsx'
import { WsErrorCodes } from '/snippets/WsErrorCodes.jsx'
import {
  channelOperations,
  channelMeta,
  positionRecord,
  exPositionsSubscribe,
  exPositionsSubscribeResponse,
  exPositionsUpdate,
  exPositionsUnsubscribe,
} from '/snippets/ws-data/positions.jsx'
import { RelatedResources } from '/components/RelatedResources.jsx'

<WsAuthBadge required={channelMeta.authRequired} />

Subscribe to collateral position updates including P&L, liquidation price, margin, and position side. **Update interval:** 1 second.

<Note>
This channel does **NOT** support query operations — only subscribe/update/unsubscribe.
</Note>

<WsChannelOverview operations={channelOperations} />

<Note>Connect to `wss://api.whitebit.com/ws` — see [WebSocket Overview](/websocket/overview) for protocol details and keepalive requirements.</Note>

## Rate limits

<WsRateLimits {...channelMeta.rateLimits} />

## Subscribe to positions

<WsSubscribeSteps
  subscribe={exPositionsSubscribe}
  subscribeNote="params is always an empty array — subscribes to all open positions."
  confirmation={exPositionsSubscribeResponse}
  update={exPositionsUpdate}
  updateNote="The server pushes positionsMargin_update with a full snapshot of all open positions every second. amount — position size (negative = short). pnl — unrealized profit and loss. liq_price — liquidation price. liq_stage — null (healthy) or margin_call. position_side — LONG, SHORT, or BOTH."
  unsubscribe={exPositionsUnsubscribe}
/>

<Tip>
Each update is a full snapshot of all open positions — local state auto-recovers within 1 second of subscribing. No gap-filling is needed after reconnection.
</Tip>

## Update frequency

The server pushes updates every **1 second** when position data changes.

## Position object

<WsSchemaTable fields={positionRecord} />

## Error codes

<WsErrorCodes errorCodes={channelMeta.errorCodes} />

<RelatedResources>
- [Margin Positions Events](/websocket/account-streams/margin-positions-events) — real-time margin call and liquidation events for collateral positions.
</RelatedResources>
