---
title: Deals
tag: "WSS"
description: "Subscribe to real-time trade deal notifications for a WhiteBIT account via WebSocket."
---

import { WsChannelOverview } from '/snippets/WsChannelOverview.jsx'
import { WsSchemaTable } from '/snippets/WsSchemaTable.jsx'
import { WsMessageExample } from '/snippets/WsMessageExample.jsx'
import { WsSubscribeSteps } from '/snippets/WsSubscribeSteps.jsx'
import { WsTupleTable } from '/snippets/WsTupleTable.jsx'
import {
  channelOperations,
  dealsUpdateParamsTupleFields,
  dealsUpdate,
  exDealsRequest,
  exDealsResponse,
  exDealsSubscribe,
  exDealsSubscribeResponse,
  exDealsUpdate,
  exDealsUnsubscribe,
} from '/snippets/ws-data/deals.jsx'

<Warning>
This is a private channel. [Authorize](/websocket/authentication) the WebSocket connection before subscribing.
</Warning>

Query or subscribe to real-time trade execution (deal) notifications. Each deal represents a matched trade with price, amount, fee, and counterparty details. **Update interval:** real-time.

<WsChannelOverview operations={channelOperations} />

## Query deals (one-time)

Fetch historical deal records for a market without subscribing.

`params`: `[market, offset, limit]`. Maximum limit: 100.

**Request**

<WsMessageExample data={exDealsRequest} />

**Response**

<WsMessageExample data={exDealsResponse} />

## Subscribe to deals

<WsSubscribeSteps
  subscribe={exDealsSubscribe}
  subscribeNote="params[0] is an array of market names. Pass an empty inner array to subscribe to all markets."
  confirmation={exDealsSubscribeResponse}
  update={exDealsUpdate}
  updateNote="The server pushes deals_update for each trade execution. The update uses a positional array format — fields are identified by index."
  updateExtra={<WsTupleTable fields={dealsUpdateParamsTupleFields} />}
  unsubscribe={exDealsUnsubscribe}
/>

## Deal object

<WsSchemaTable fields={dealsUpdate} />
