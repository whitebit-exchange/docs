---
title: Borrows Events
tag: "WSS"
description: "Subscribe to real-time margin borrow events for a WhiteBIT account via WebSocket."
---

import { WsChannelOverview } from '/snippets/WsChannelOverview.jsx'
import { WsSchemaTable } from '/snippets/WsSchemaTable.jsx'
import { WsMessageExample } from '/snippets/WsMessageExample.jsx'
import { WsSubscribeSteps } from '/snippets/WsSubscribeSteps.jsx'
import { WsTupleTable } from '/snippets/WsTupleTable.jsx'
import { WsAuthBadge } from '/snippets/WsAuthBadge.jsx'
import { WsRateLimits } from '/snippets/WsRateLimits.jsx'
import { WsErrorCodes } from '/snippets/WsErrorCodes.jsx'
import {
  channelOperations,
  channelMeta,
  borrowsEventsUpdateParamsTupleFields,
  borrowRecord,
  exBorrowsEventsSubscribe,
  exBorrowsEventsSubscribeResponse,
  exBorrowsEventsUpdate,
  exBorrowsEventsUnsubscribe,
} from '/snippets/ws-data/borrows_events.jsx'
import { RelatedResources } from '/components/RelatedResources.jsx'

<WsAuthBadge required={channelMeta.authRequired} />

Subscribe to margin call and liquidation event notifications for collateral borrows. Events fire when a borrow enters margin call or liquidation state. This is an event-driven channel — updates are pushed once per state change, with no periodic polling or snapshot on subscribe.

<WsChannelOverview operations={channelOperations} />

<Note>Connect to `wss://api.whitebit.com/ws` — see [WebSocket Overview](/websocket/overview) for protocol details and keepalive requirements.</Note>

## Rate limits

<WsRateLimits {...channelMeta.rateLimits} />

## Subscribe to borrow events

<WsSubscribeSteps
  subscribe={exBorrowsEventsSubscribe}
  subscribeNote="params is always an empty array."
  confirmation={exBorrowsEventsSubscribeResponse}
  update={exBorrowsEventsUpdate}
  updateNote="params[0] is the event type. params[1] is the borrow object."
  updateExtra={<WsTupleTable fields={borrowsEventsUpdateParamsTupleFields} />}
  unsubscribe={exBorrowsEventsUnsubscribe}
/>

<Note>
This channel delivers events only on state changes — no snapshot is sent on subscribe, and events missed during a disconnection are not replayed. For complete borrow state awareness, pair this channel with [Borrows](/websocket/account-streams/borrows), which sends full snapshots every 1.5 seconds.
</Note>

## Update frequency

Updates are pushed in **real time** on borrow state changes (new borrow, repayment, liquidation).

## Error codes

<WsErrorCodes errorCodes={channelMeta.errorCodes} />

## Borrow event object

<WsSchemaTable fields={borrowRecord} />

<RelatedResources>
- [Borrows](/websocket/account-streams/borrows) — current borrow state with periodic full snapshots.
</RelatedResources>
