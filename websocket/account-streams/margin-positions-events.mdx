---
title: Margin Positions Events
tag: "WSS"
description: "Subscribe to real-time margin position change events for a WhiteBIT account via WebSocket."
---

import { WsChannelOverview } from '/snippets/WsChannelOverview.jsx'
import { WsSchemaTable } from '/snippets/WsSchemaTable.jsx'
import { WsMessageExample } from '/snippets/WsMessageExample.jsx'
import { WsSubscribeSteps } from '/snippets/WsSubscribeSteps.jsx'
import {
  positionRecord,
  exMarginPositionsEventsSubscribe,
  exMarginPositionsEventsSubscribeResponse,
  exMarginPositionsEventsUpdate,
  exMarginPositionsEventsUnsubscribe,
} from '/snippets/ws-data/margin_positions_events.jsx'

<Warning>
This is a private channel. [Authorize](/websocket/authentication) the WebSocket connection before subscribing.
</Warning>

Subscribe to margin call and liquidation event notifications for collateral positions. Events fire when a position enters margin call or liquidation state.

<WsChannelOverview operations={[
  {
    name: "Subscribe",
    send: "positionsAccountMargin_subscribe",
    receive: "Confirmation (status: success)",
    push: "positionsAccountMargin_update — margin call or liquidation event"
  },
  {
    name: "Unsubscribe",
    send: "positionsAccountMargin_unsubscribe",
    receive: "Confirmation (status: success)",
    push: null
  }
]} />

## Subscribe to position events

<WsSubscribeSteps
  subscribe={exMarginPositionsEventsSubscribe}
  subscribeNote="params is always an empty array."
  confirmation={exMarginPositionsEventsSubscribeResponse}
  update={exMarginPositionsEventsUpdate}
  updateNote="params[0] is the event type. params[1] is the position object."
  updateExtra={
    <div>
      <div style={{width: '100%', fontSize: '0.875rem', marginBottom: '1rem'}}>
        <div style={{display: 'flex', borderBottom: '1px solid #e5e7eb', fontWeight: 600}}>
          <div style={{padding: '8px 12px', flex: '0 0 80px'}}>Value</div>
          <div style={{padding: '8px 12px', flex: 1}}>Event</div>
        </div>
        <div style={{display: 'flex', borderBottom: '1px solid #f3f4f6'}}>
          <div style={{padding: '8px 12px', flex: '0 0 80px'}}><code>1</code></div>
          <div style={{padding: '8px 12px', flex: 1}}>Margin call</div>
        </div>
        <div style={{display: 'flex'}}>
          <div style={{padding: '8px 12px', flex: '0 0 80px'}}><code>2</code></div>
          <div style={{padding: '8px 12px', flex: 1}}>Liquidation</div>
        </div>
      </div>
      <div style={{borderLeft: '3px solid #60a5fa', paddingLeft: '0.75rem', marginTop: '0.75rem', fontSize: '0.8125rem', color: '#374151', lineHeight: '1.5'}}>
        <code>liq_stage</code> values: <code>margin_call</code>, <code>liquidation</code>, or <code>null</code> (healthy).
      </div>
    </div>
  }
  unsubscribe={exMarginPositionsEventsUnsubscribe}
/>

## Position event object

<WsSchemaTable fields={positionRecord} />
