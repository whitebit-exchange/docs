---
title: Orders Executed
tag: "WSS"
description: "Subscribe to real-time notifications when account orders are executed on WhiteBIT via WebSocket."
---

import { WsChannelOverview } from '/snippets/WsChannelOverview.jsx'
import { WsSchemaTable } from '/snippets/WsSchemaTable.jsx'
import { WsMessageExample } from '/snippets/WsMessageExample.jsx'
import { WsSubscribeSteps } from '/snippets/WsSubscribeSteps.jsx'
import { WsTupleTable } from '/snippets/WsTupleTable.jsx'
import { WsAuthBadge } from '/snippets/WsAuthBadge.jsx'
import { WsRateLimits } from '/snippets/WsRateLimits.jsx'
import { WsErrorCodes } from '/snippets/WsErrorCodes.jsx'
import {
  channelOperations,
  channelMeta,
  executedOrderObject,
  ordersExecutedRequestParamsTupleFields,
  ordersExecutedSubscribeParamsTupleFields,
  exOrdersExecutedRequest,
  exOrdersExecutedResponse,
  exOrdersExecutedSubscribe,
  exOrdersExecutedSubscribeResponse,
  exOrdersExecutedUpdate,
  exOrdersExecutedUnsubscribe,
} from '/snippets/ws-data/orders_executed.jsx'
import { RelatedResources } from '/components/RelatedResources.jsx'

<Note>
The `reduce_only` response field documented on this page is **coming soon**. The documentation is published in advance for integration planning. The field is not yet returned by the API. See [Reduce-only orders](/concepts/order-types#reduce-only-orders) for details.
</Note>

<WsAuthBadge required={channelMeta.authRequired} />

Query or subscribe to executed order history with optional filtering by order type (limit, market, or both). Each record includes execution details, fees, and status. **Update interval:** real-time.

<Note>Connect to `wss://api.whitebit.com/ws` — see [WebSocket Overview](/websocket/overview) for protocol details and keepalive requirements.</Note>

## Rate limits

<WsRateLimits {...channelMeta.rateLimits} />

<WsChannelOverview operations={channelOperations} />

## Query executed orders (one-time)

Fetch historical executed orders for a market without subscribing.

<WsTupleTable fields={ordersExecutedRequestParamsTupleFields} />

**Request**

<WsMessageExample data={exOrdersExecutedRequest} />

**Response**

<WsMessageExample data={exOrdersExecutedResponse} />

<Note>
The private stream returns executed orders, including orders using Retail Price Improvement (RPI) mode.
</Note>

## Subscribe to executed orders

<WsTupleTable fields={ordersExecutedSubscribeParamsTupleFields} />

<WsSubscribeSteps
  subscribe={exOrdersExecutedSubscribe}
  confirmation={exOrdersExecutedSubscribeResponse}
  update={exOrdersExecutedUpdate}
  unsubscribe={exOrdersExecutedUnsubscribe}
/>

<Note>
Execution events during a disconnection are not replayed. After reconnecting, use `ordersExecuted_request` to query recent executed orders, or call the REST [Executed Order History](/api-reference/spot-trading/query-executed-order-history) endpoint to backfill missed records. See [Reconnection and state recovery](/guides/websocket-quickstart#state-recovery-after-reconnect).
</Note>

## Update frequency

Updates are pushed in **real time** when an order is fully executed or canceled.

## Error codes

<WsErrorCodes errorCodes={channelMeta.errorCodes} />

## Executed order object

<WsSchemaTable fields={executedOrderObject} />

<RelatedResources>
- [Orders Pending](/websocket/account-streams/orders-pending) — stream of open (unexecuted) orders.
- [Deals](/websocket/account-streams/deals) — individual trade fills for each executed order.
</RelatedResources>
