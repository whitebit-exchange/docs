---
title: Orders Pending
tag: "WSS"
description: "Subscribe to real-time updates on pending account orders on WhiteBIT via WebSocket."
---

import { WsChannelOverview } from '/snippets/WsChannelOverview.jsx'
import { WsSchemaTable } from '/snippets/WsSchemaTable.jsx'
import { WsMessageExample } from '/snippets/WsMessageExample.jsx'
import { WsSubscribeSteps } from '/snippets/WsSubscribeSteps.jsx'
import { WsTupleTable } from '/snippets/WsTupleTable.jsx'
import { WsAuthBadge } from '/snippets/WsAuthBadge.jsx'
import { WsRateLimits } from '/snippets/WsRateLimits.jsx'
import { WsErrorCodes } from '/snippets/WsErrorCodes.jsx'
import {
  channelOperations,
  channelMeta,
  ordersPendingRequestParamsTupleFields,
  ordersPendingUpdateParamsTupleFields,
  orderObject,
  exOrdersPendingRequest,
  exOrdersPendingResponse,
  exOrdersPendingSubscribe,
  exOrdersPendingSubscribeResponse,
  exOrdersPendingUpdate,
  exOrdersPendingUnsubscribe,
} from '/snippets/ws-data/orders_pending.jsx'
import { RelatedResources } from '/components/RelatedResources.jsx'

<Note>
The `reduce_only` response field documented on this page is **coming soon**. The documentation is published in advance for integration planning. The field is not yet returned by the API. See [Reduce-only orders](/concepts/order-types#reduce-only-orders) for details.
</Note>

<WsAuthBadge required={channelMeta.authRequired} />

Query or subscribe to active (unexecuted) orders across specified markets. The server pushes `ordersPending_update` events in real time on every order state change. Subscribe to multiple markets simultaneously by passing market names in the `params` array.

<WsChannelOverview operations={channelOperations} />

<Note>Connect to `wss://api.whitebit.com/ws` — see [WebSocket Overview](/websocket/overview) for protocol details and keepalive requirements.</Note>

## Rate limits

<WsRateLimits {...channelMeta.rateLimits} />

## Query pending orders (one-time)

Fetch current pending orders for a market without subscribing to updates.

<WsTupleTable fields={ordersPendingRequestParamsTupleFields} />

**Request**

<WsMessageExample data={exOrdersPendingRequest} />

**Response**

<WsMessageExample data={exOrdersPendingResponse} />

## Subscribe to order updates

<WsSubscribeSteps
  subscribe={exOrdersPendingSubscribe}
  subscribeNote="Subscribe to one or more markets. Pass market names in params."
  confirmation={exOrdersPendingSubscribeResponse}
  update={exOrdersPendingUpdate}
  updateNote="The server pushes ordersPending_update on every order change — no further requests needed."
  updateExtra={
    <div>
      <p><code>params[0]</code> is the update event ID. <code>params[1]</code> is the Order object — see <a href="#order-object">Order object</a> below.</p>
      <WsTupleTable fields={ordersPendingUpdateParamsTupleFields} />
      <div style={{borderLeft: '3px solid #60a5fa', paddingLeft: '0.75rem', marginTop: '0.75rem', fontSize: '0.8125rem', color: '#374151', lineHeight: '1.5'}}>
        If a new order instantly matches an existing order from the order book, only one message is received — with event ID <code>3</code>.
      </div>
    </div>
  }
  unsubscribe={exOrdersPendingUnsubscribe}
/>

## Update frequency

Updates are pushed in **real time** on every order state change (new, modified, partially filled).

## Error codes

<WsErrorCodes errorCodes={channelMeta.errorCodes} />

## Order modification behavior

When `POST /api/v4/order/modify` is called, the matching engine cancels the original order and creates a replacement, producing **two sequential events**:

1. `event_id=3` (Finish) — original order cancelled. The `id` in the payload matches the old `order_id`.
2. `event_id=1` (New) — replacement order created with a **new `order_id`**.

Clients tracking orders by `order_id` must update the stored value after each modify call. Use `client_order_id` for stable correlation across modifications.

<div style={{borderLeft: '3px solid #60a5fa', paddingLeft: '0.75rem', marginTop: '0.75rem', fontSize: '0.8125rem', color: '#374151', lineHeight: '1.5'}}>
  The private stream returns active orders, including orders using Retail Price Improvement (RPI) mode.
</div>

## Order object

Fields present in both the query response (`records[]`) and the pushed `ordersPending_update` payload.

<WsSchemaTable fields={orderObject} />

<RelatedResources>
- [Orders Executed](/websocket/account-streams/orders-executed) — stream of filled and cancelled orders.
</RelatedResources>
