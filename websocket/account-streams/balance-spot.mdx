---
title: Balance Spot
tag: "WSS"
description: "Subscribe to real-time spot balance updates for a WhiteBIT account via WebSocket."
---

import { WsChannelOverview } from '/snippets/WsChannelOverview.jsx'
import { WsSchemaTable } from '/snippets/WsSchemaTable.jsx'
import { WsMessageExample } from '/snippets/WsMessageExample.jsx'
import { WsSubscribeSteps } from '/snippets/WsSubscribeSteps.jsx'
import {
  channelOperations,
  balanceSpotUpdate,
  exBalanceSpotRequest,
  exBalanceSpotResponse,
  exBalanceSpotSubscribe,
  exBalanceSpotSubscribeResponse,
  exBalanceSpotUpdate,
  exBalanceSpotUnsubscribe,
} from '/snippets/ws-data/balance_spot.jsx'

<Warning>
This is a private channel. [Authorize](/websocket/authentication) the WebSocket connection before subscribing.
</Warning>

Get and subscribe to [spot balance](/glossary#balance-spotbalance-trade) updates. **Update interval:** 1 second.

<WsChannelOverview operations={channelOperations} />

## Query spot balance (one-time)

Fetch current spot balances for specific assets without subscribing.

`params`: `[asset, asset, ...]`. Pass asset tickers to query specific balances. Pass an empty array to query all assets.

**Request**

<WsMessageExample data={exBalanceSpotRequest} />

**Response**

<WsMessageExample data={exBalanceSpotResponse} />

## Subscribe to spot balance

<WsSubscribeSteps
  subscribe={exBalanceSpotSubscribe}
  subscribeNote="Pass asset tickers in params. Pass an empty array to subscribe to all assets."
  confirmation={exBalanceSpotSubscribeResponse}
  update={exBalanceSpotUpdate}
  updateNote="The server pushes balanceSpot_update when any subscribed asset balance changes. available — amount free to trade. freeze — amount locked in active orders."
  unsubscribe={exBalanceSpotUnsubscribe}
/>

## Balance object

<WsSchemaTable fields={balanceSpotUpdate} />
