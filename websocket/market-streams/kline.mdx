---
title: Kline
tag: "WSS"
description: "Subscribe to real-time candlestick (OHLCV) chart updates for a trading pair via WhiteBIT WebSocket."
---

import { WsChannelOverview } from '/snippets/WsChannelOverview.jsx'
import { WsSchemaTable } from '/snippets/WsSchemaTable.jsx'
import { WsMessageExample } from '/snippets/WsMessageExample.jsx'
import { WsSubscribeSteps } from '/snippets/WsSubscribeSteps.jsx'
import {
  candlesUpdate,
  exCandlesRequest,
  exCandlesResponse,
  exCandlesSubscribe,
  exCandlesSubscribeResponse,
  exCandlesUpdate,
  exCandlesUnsubscribe,
} from '/snippets/ws-data/kline.jsx'

Query or subscribe to candlestick (OHLCV) data for a market and interval. **Update interval:** 0.5 seconds.

<WsChannelOverview operations={[
  {
    name: "Query",
    send: "candles_request",
    receive: "Array of candlestick records",
    push: null
  },
  {
    name: "Subscribe",
    send: "candles_subscribe",
    receive: "Confirmation (status: success)",
    push: "candles_update — latest candle for the subscribed interval"
  },
  {
    name: "Unsubscribe",
    send: "candles_unsubscribe",
    receive: "Confirmation (status: success)",
    push: null
  }
]} />

## Query candlesticks (one-time)

Fetch historical candlestick data for a time range.

`params`: `[market, start_time, end_time, interval]`. Times are Unix timestamps.

**Request**

<WsMessageExample data={exCandlesRequest} />

**Response**

<WsMessageExample data={exCandlesResponse} />

Each record is a positional array in the same format as the subscription update.

## Interval constraints

The interval must meet specific alignment rules to ensure candles align with natural time boundaries.

<Accordion title="Less than 60 seconds">
  60 must be divisible by the interval.

  Valid: `1`, `2`, `3`, `4`, `5`, `6`, `10`, `12`, `15`, `20`, `30`

  Invalid: `7`, `11`, `13`, `17`
</Accordion>

<Accordion title="60 seconds to 1 hour (60–3599s)">
  Interval must be divisible by 60, and 3600 must be divisible by the interval.

  Valid: `60` (1m), `120` (2m), `180` (3m), `300` (5m), `900` (15m), `1800` (30m)

  Invalid: `420` (7m), `660` (11m)
</Accordion>

<Accordion title="1 hour to 1 day (3600–86399s)">
  Interval must be divisible by 3600, and 86400 must be divisible by the interval.

  Valid: `3600` (1h), `7200` (2h), `14400` (4h), `21600` (6h), `43200` (12h)

  Invalid: `18000` (5h), `25200` (7h)
</Accordion>

<Accordion title="1 day and above">
  Valid: `86400` (1d), `172800` (2d), `259200` (3d), `604800` (1w), `2592000` (1mo)
</Accordion>

<Note>
The constraints above ensure candles align with natural time boundaries (minutes, hours, days) for consistent charting.
</Note>

## Subscribe to candlesticks

<WsSubscribeSteps
  subscribe={exCandlesSubscribe}
  subscribeNote="params[0] is the market. params[1] is the interval in seconds. See Interval constraints above."
  confirmation={exCandlesSubscribeResponse}
  update={exCandlesUpdate}
  updateNote="The server pushes candles_update every 0.5 seconds with the current candle. The update payload is a positional array — fields are identified by index."
  updateExtra={
    <div style={{width: '100%', fontSize: '0.875rem', marginBottom: '1rem'}}>
      <div style={{display: 'flex', borderBottom: '1px solid #e5e7eb', fontWeight: 600}}>
        <div style={{padding: '8px 12px', flex: '0 0 70px'}}>Index</div>
        <div style={{padding: '8px 12px', flex: '0 0 120px'}}>Field</div>
        <div style={{padding: '8px 12px', flex: 1}}>Description</div>
      </div>
      <div style={{display: 'flex', borderBottom: '1px solid #f3f4f6'}}>
        <div style={{padding: '8px 12px', flex: '0 0 70px', whiteSpace: 'nowrap'}}><code>[0]</code></div>
        <div style={{padding: '8px 12px', flex: '0 0 120px'}}>time</div>
        <div style={{padding: '8px 12px', flex: 1}}>Candle open time (Unix timestamp)</div>
      </div>
      <div style={{display: 'flex', borderBottom: '1px solid #f3f4f6'}}>
        <div style={{padding: '8px 12px', flex: '0 0 70px', whiteSpace: 'nowrap'}}><code>[1]</code></div>
        <div style={{padding: '8px 12px', flex: '0 0 120px'}}>open</div>
        <div style={{padding: '8px 12px', flex: 1}}>Open price</div>
      </div>
      <div style={{display: 'flex', borderBottom: '1px solid #f3f4f6'}}>
        <div style={{padding: '8px 12px', flex: '0 0 70px', whiteSpace: 'nowrap'}}><code>[2]</code></div>
        <div style={{padding: '8px 12px', flex: '0 0 120px'}}>close</div>
        <div style={{padding: '8px 12px', flex: 1}}>Close price (current price for live candle)</div>
      </div>
      <div style={{display: 'flex', borderBottom: '1px solid #f3f4f6'}}>
        <div style={{padding: '8px 12px', flex: '0 0 70px', whiteSpace: 'nowrap'}}><code>[3]</code></div>
        <div style={{padding: '8px 12px', flex: '0 0 120px'}}>high</div>
        <div style={{padding: '8px 12px', flex: 1}}>Highest price in the interval</div>
      </div>
      <div style={{display: 'flex', borderBottom: '1px solid #f3f4f6'}}>
        <div style={{padding: '8px 12px', flex: '0 0 70px', whiteSpace: 'nowrap'}}><code>[4]</code></div>
        <div style={{padding: '8px 12px', flex: '0 0 120px'}}>low</div>
        <div style={{padding: '8px 12px', flex: 1}}>Lowest price in the interval</div>
      </div>
      <div style={{display: 'flex', borderBottom: '1px solid #f3f4f6'}}>
        <div style={{padding: '8px 12px', flex: '0 0 70px', whiteSpace: 'nowrap'}}><code>[5]</code></div>
        <div style={{padding: '8px 12px', flex: '0 0 120px'}}>volume</div>
        <div style={{padding: '8px 12px', flex: 1}}>Volume in stock (base currency)</div>
      </div>
      <div style={{display: 'flex', borderBottom: '1px solid #f3f4f6'}}>
        <div style={{padding: '8px 12px', flex: '0 0 70px', whiteSpace: 'nowrap'}}><code>[6]</code></div>
        <div style={{padding: '8px 12px', flex: '0 0 120px'}}>deal</div>
        <div style={{padding: '8px 12px', flex: 1}}>Volume in money (quote currency)</div>
      </div>
      <div style={{display: 'flex'}}>
        <div style={{padding: '8px 12px', flex: '0 0 70px', whiteSpace: 'nowrap'}}><code>[7]</code></div>
        <div style={{padding: '8px 12px', flex: '0 0 120px'}}>market</div>
        <div style={{padding: '8px 12px', flex: 1}}>Market name</div>
      </div>
    </div>
  }
  unsubscribe={exCandlesUnsubscribe}
/>

## Candle object

<WsSchemaTable fields={candlesUpdate} />
