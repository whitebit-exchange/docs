---
asyncapi: "/asyncapi/public/service.yaml /ws"
title: Service
description: "Ping the WhiteBIT WebSocket server to check connection health and keep the connection alive."
---

Check API availability with ping/pong requests and query the current server time. The Service channel supports one-time queries only and does not emit subscription update events.
