---
title: Service
tag: "WSS"
description: "Ping the WhiteBIT WebSocket server to check connection health and keep the connection alive."
---

import { WsChannelOverview } from '/snippets/WsChannelOverview.jsx'
import { WsMessageExample } from '/snippets/WsMessageExample.jsx'
import { WsAuthBadge } from '/snippets/WsAuthBadge.jsx'
import { WsRateLimits } from '/snippets/WsRateLimits.jsx'
import { WsErrorCodes } from '/snippets/WsErrorCodes.jsx'
import {
  channelOperations,
  channelMeta,
  exPingRequest,
  exPingResponse,
  exTimeRequest,
  exTimeResponse,
} from '/snippets/ws-data/service.jsx'

Check API availability with ping/pong and query the current server time. The Service channel supports one-time queries only — no subscription or push events. Use ping to verify connection health and retrieve the server timestamp for clock synchronization.

<WsAuthBadge required={channelMeta.authRequired} />

<WsChannelOverview operations={channelOperations} />

<Note>Connect to `wss://api.whitebit.com/ws` — see [WebSocket Overview](/websocket/overview) for protocol details and keepalive requirements.</Note>

## Rate limits

<WsRateLimits {...channelMeta.rateLimits} />

## Update frequency

Client-initiated via `server.time` request. The server enforces a **60-second** ping timeout — send a ping within 60 seconds to keep the connection alive.

## Error codes

<WsErrorCodes errorCodes={channelMeta.errorCodes} />

## Ping

Send a ping every 50 seconds to keep the connection alive. The server closes the connection after 60 seconds of inactivity.

**Request**

<WsMessageExample data={exPingRequest} />

**Response**

<WsMessageExample data={exPingResponse} />

## Server time

**Request**

<WsMessageExample data={exTimeRequest} />

**Response**

<WsMessageExample data={exTimeResponse} />

`result` is the current server time as a Unix timestamp (integer seconds).
