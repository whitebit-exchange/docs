# AI pipeline working artifacts (page-slug subdirs only; README stays in repo)
ai/outputs/review/*/

# Local DevRel toolkit (partner analysis/research/briefs) — not for the repo
devrel/

# Local-only backlog note — kept on disk, not tracked
ai/backlog/examples-needed.md

# Dependencies
node_modules/

# macOS
.DS_Store
